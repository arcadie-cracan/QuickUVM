//----------------------------------------------------------------------
// This File: rsp_seq_item.svh
// Generated by: QuickUVM
// Project:  reqrsp
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class rsp_seq_item extends uvm_sequence_item;
  `uvm_object_utils(rsp_seq_item)

  // Output fields (not randomized — sampled from DUT)
       logic rsp_valid;
       logic [3:0] rsp_id;
       logic [7:0] rsp_data;

  // Input fields (driven to DUT)

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name="rsp_seq_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    rsp_seq_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("rsp_seq_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    rsp_valid = tr.rsp_valid;
    rsp_id = tr.rsp_id;
    rsp_data = tr.rsp_data;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    rsp_seq_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("rsp_seq_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (rsp_valid === tr.rsp_valid);
    eq &= (rsp_id === tr.rsp_id);
    eq &= (rsp_data === tr.rsp_data);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("rsp_valid=%b  ", rsp_valid)};
    s = {s, $sformatf("rsp_id=%b  ", rsp_id)};
    s = {s, $sformatf("rsp_data=%0h  ", rsp_data)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
