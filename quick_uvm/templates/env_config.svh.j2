//----------------------------------------------------------------------
// This File: {{ dut.name }}_env_cfg.svh
// Generated by: QuickUVM
// Project:  {{ project.name }}
// Author:   {{ project.author }}
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// Environment configuration object. Aggregates the per-agent configuration
// handles. {{ dut.name }}_base_test creates and populates this, sets it into the config DB
// for the environment, and the environment distributes each agent config.
//----------------------------------------------------------------------

class {{ dut.name }}_env_cfg extends uvm_object;
  `uvm_object_utils({{ dut.name }}_env_cfg)

  // Per-agent configuration handles
{% for agent in agents %}
  {{ agent.name }}_cfg {{ agent.name }}_cfg;
{% endfor %}
{% if register_model %}

  // Register model (built and locked by {{ dut.name }}_base_test)
  {{ register_model.block }} reg_model;
{% endif %}

  // pragma quickuvm custom config_var_additional begin
  // pragma quickuvm custom config_var_additional end

  function new (string name = "{{ dut.name }}_env_cfg");
    super.new(name);
  endfunction

endclass
