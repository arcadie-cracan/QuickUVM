//----------------------------------------------------------------------
// This File: wr_item.svh
// Generated by: QuickUVM
// Project:  axi_write_tb
// Author:   qa@example.com
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class wr_item extends uvm_sequence_item;
  `uvm_object_utils(wr_item)

  // Output fields (not randomized — sampled from DUT)
       logic awvalid;
       logic [3:0] awid;
       logic [31:0] awaddr;
       logic wvalid;
       logic [31:0] wdata;
       logic wlast;

  // Input fields (driven to DUT)
  rand bit bvalid;
  rand bit [3:0] bid;
  rand bit [1:0] bresp;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name="wr_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    wr_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("wr_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    awvalid = tr.awvalid;
    awid = tr.awid;
    awaddr = tr.awaddr;
    wvalid = tr.wvalid;
    wdata = tr.wdata;
    wlast = tr.wlast;
    bvalid = tr.bvalid;
    bid = tr.bid;
    bresp = tr.bresp;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    wr_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("wr_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (awvalid === tr.awvalid);
    eq &= (awid === tr.awid);
    eq &= (awaddr === tr.awaddr);
    eq &= (wvalid === tr.wvalid);
    eq &= (wdata === tr.wdata);
    eq &= (wlast === tr.wlast);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    s = {s, $sformatf("bvalid=%b  ", bvalid)};
    s = {s, $sformatf("bid=%b  ", bid)};
    s = {s, $sformatf("bresp=%b  ", bresp)};
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("awvalid=%b  ", awvalid)};
    s = {s, $sformatf("awid=%b  ", awid)};
    s = {s, $sformatf("awaddr=%0h  ", awaddr)};
    s = {s, $sformatf("wvalid=%b  ", wvalid)};
    s = {s, $sformatf("wdata=%0h  ", wdata)};
    s = {s, $sformatf("wlast=%b  ", wlast)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
