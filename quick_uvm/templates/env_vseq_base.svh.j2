//----------------------------------------------------------------------
// This File: {{ dut.name }}_base_vseq.svh
// Generated by: QuickUVM
// Project:  {{ project.name }}
// Author:   {{ project.author }}
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// C2 virtual sequence base — runs on the {{ dut.name }}_virtual_sequencer, so `p_sequencer` exposes the
// per-agent sequencer handles. Concrete virtual sequences extend this.

class {{ dut.name }}_base_vseq extends uvm_sequence #(uvm_sequence_item);
  `uvm_object_utils({{ dut.name }}_base_vseq)
  `uvm_declare_p_sequencer({{ dut.name }}_virtual_sequencer)

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name = "{{ dut.name }}_base_vseq");
    super.new(name);
  endfunction

endclass
