//----------------------------------------------------------------------
// This File: nchan_ch_2_predictor.svh
// Generated by: QuickUVM
// Project:  nchan_tb
// Author:   qa@example.com
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// The predictor:
//   - subscribes to the monitor's analysis port
//   - ignores sampled DUT outputs
//   - calls predict() to predict the expected output
//   - writes the predicted transaction to results_ap
//----------------------------------------------------------------------

class nchan_ch_2_predictor extends uvm_subscriber #(ch_item);
  `uvm_component_utils(nchan_ch_2_predictor)

  uvm_analysis_port #(ch_item) results_ap;

  // pragma quickuvm custom class_item_additional begin
  bit m_last;   // the last d captured on v (this channel's latched value)
  // pragma quickuvm custom class_item_additional end

  function new(string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    results_ap = new("results_ap", this);
  endfunction

  function void write(ch_item t);
    ch_item exp_tr;
    exp_tr = predict(t);
    results_ap.write(exp_tr);
  endfunction

  extern function ch_item predict(ch_item t);

endclass
