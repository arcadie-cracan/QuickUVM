//----------------------------------------------------------------------
// This File: lo_s_driver.svh
// Generated by: QuickUVM
// Project:  shl
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class lo_s_driver #(parameter int W = 8) extends uvm_driver #(lo_s_item#(W));
  `uvm_component_param_utils(lo_s_driver#(W))

  virtual lo_s_if#(W) vif;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  task run_phase(uvm_phase phase);
    lo_s_item#(W) tr;
    initialize();
    forever begin
      `uvm_info("DEBUG: lo_s_driver Run", "... getting next item ...", UVM_FULL)
      seq_item_port.get_next_item(tr);
      drive_item(tr);
      seq_item_port.item_done();
      `uvm_info("DEBUG: lo_s_driver Run", "... next item done ...", UVM_FULL)
    end
  endtask

  virtual task initialize (); // @0 - Does not use clocking block
    `uvm_info("INIT", "lo_s_driver: Initialize (time @0)", UVM_HIGH)
    vif.din <= '0;
    @vif.cb1;
    // pragma quickuvm custom initialize_additional begin
    // pragma quickuvm custom initialize_additional end
  endtask

  virtual task drive_item (lo_s_item#(W) tr);
    `uvm_info("drive_item", tr.input2string(), UVM_FULL)
    vif.cb1.din <= tr.din;
    @vif.cb1;
    // pragma quickuvm custom drive_item_additional begin
    // pragma quickuvm custom drive_item_additional end
  endtask

endclass
