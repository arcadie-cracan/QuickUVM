//----------------------------------------------------------------------
// This File: sat_adder_reference_model.c
// Generated by: QuickUVM
// Project:  sat_adder_tb
// Author:   qa@example.com
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// K0 — the DUT's golden / reference model, in C (called per transaction via
// DPI-C from sat_adder_reference_model.svh). Inputs are passed by value;
// write the EXPECTED DUT outputs through the output pointers. This is the only
// reference-model file you edit — the SV bridge + signature are generated.
//----------------------------------------------------------------------
#include "svdpi.h"

void sat_adder_predict(
    char a,
    char b,
    char *sum,
    char *ovf
) {
  // pragma quickuvm custom reference_model begin
  // Unsigned saturating add: sum = min(a + b, 0xFF); ovf on 8-bit overflow.
  int s = (unsigned char)a + (unsigned char)b;
  *ovf = (s > 0xFF) ? 1 : 0;
  *sum = (char)((s > 0xFF) ? 0xFF : s);
  // pragma quickuvm custom reference_model end
}
