//----------------------------------------------------------------------
// This File: {{ dut.name }}_virtual_sequencer.svh
// Generated by: QuickUVM
// Project:  {{ project.name }}
// Author:   {{ project.author }}
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// Top (subsystem) virtual sequencer — holds a handle to each composed block's
// agent sequencer, so a top virtual sequence reaches them via p_sequencer.
//----------------------------------------------------------------------

class {{ dut.name }}_virtual_sequencer extends uvm_sequencer;
  `uvm_component_utils({{ dut.name }}_virtual_sequencer)

  // Handles to each composed block's active agent sequencers
{% for sv in subenvs %}
{% if sv.is_subsystem %}
  {{ sv.vsqr_type }} {{ sv.name }}_vsqr;
{% else %}
{% for agent in sv.agents if agent.active %}
  {{ agent.name }}_sequencer{{ agent.param_args_values }} {{ sv.name }}_{{ agent.name }}_sqr;
{% endfor %}
{% endif %}
{% endfor %}
{% for agent in boundary_agents if agent.active %}
  {{ agent.name }}_sequencer {{ agent.name }}_sqr;
{% endfor %}

  function new (string name, uvm_component parent);
    super.new(name, parent);
  endfunction

endclass
