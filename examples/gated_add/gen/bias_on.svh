//----------------------------------------------------------------------
// This File: bias_on.svh
// Generated by: QuickUVM
// Project:  gated_add
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// S2 library sequence — kind 'directed' for agent 'src'.

class bias_on extends uvm_sequence #(src_seq_item);
  `uvm_object_utils(bias_on)

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name = "bias_on");
    super.new(name);
  endfunction

  // Item count — a test may override it via `sequence: {... count: N}` (S2).
  int unsigned count = 40;

  // 'directed' sequence — write the body below. The default skeleton
  // drives `count` randomized item(s); replace it with the
  // directed stimulus.
  task body;
    src_seq_item tr = src_seq_item::type_id::create("tr");
    `uvm_info("body", "bias_on (directed): starting", UVM_FULL)
    // pragma quickuvm custom body begin
    repeat(count) begin
      start_item(tr);
      tr.bias.rand_mode(1);   // re-enable bias for this test (off by default)
      if (!(tr.randomize()))
        `uvm_fatal("BIAS_ON", "bias_on randomization failed")
      finish_item(tr);
    end
    // pragma quickuvm custom body end
  endtask

endclass
