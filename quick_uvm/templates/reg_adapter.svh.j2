//----------------------------------------------------------------------
// This File: {{ register_model.adapter }}.svh
// Generated by: QuickUVM
// Project:  {{ project.name }}
// Author:   {{ project.author }}
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// Front-door register adapter: converts uvm_reg_bus_op <-> {{ reg_bus_agent.transaction }}.
// The protocol-specific mapping is YOUR code — fill the reg2bus / bus2reg
// pragma sections below (field names, opcodes, paging, byte ordering).
//----------------------------------------------------------------------
{% set ba = reg_bus_agent %}

class {{ register_model.adapter }} extends uvm_reg_adapter;
  `uvm_object_utils({{ register_model.adapter }})

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name = "{{ register_model.adapter }}");
    super.new(name);
    // Set provides_responses = 1 if read data is returned in a response item.
    // pragma quickuvm custom new_additional begin
    // pragma quickuvm custom new_additional end
  endfunction

  // Register operation -> bus transaction
  virtual function uvm_sequence_item reg2bus(const ref uvm_reg_bus_op rw);
    {{ ba.transaction }} tr = {{ ba.transaction }}::type_id::create("reg2bus_tr");
    // pragma quickuvm custom reg2bus begin
    // TODO: map rw.kind (UVM_READ/UVM_WRITE), rw.addr, rw.data onto tr fields, e.g.:
    //   tr.cmd_opcode = (rw.kind == UVM_WRITE) ? OP_WRITE : OP_READ;
    //   tr.cmd_addr   = rw.addr[7:0];
    //   tr.cmd_data   = rw.data[7:0];
    // pragma quickuvm custom reg2bus end
    return tr;
  endfunction

  // Bus transaction -> register operation
  virtual function void bus2reg(uvm_sequence_item bus_item, ref uvm_reg_bus_op rw);
    {{ ba.transaction }} tr;
    if (!$cast(tr, bus_item)) begin
      `uvm_fatal("{{ register_model.adapter | upper }}", "bus2reg: unexpected bus item type")
      return;
    end
    // pragma quickuvm custom bus2reg begin
    // TODO: map tr fields onto rw.kind / rw.addr / rw.data / rw.status, e.g.:
    //   rw.kind   = (tr.cmd_opcode == OP_WRITE) ? UVM_WRITE : UVM_READ;
    //   rw.addr   = tr.cmd_addr;
    //   rw.data   = tr.rsp_data;
    //   rw.status = UVM_IS_OK;
    // pragma quickuvm custom bus2reg end
  endfunction

endclass
