//----------------------------------------------------------------------
// This File: ahb_seq_item.svh
// Generated by: QuickUVM
// Project:  ahb_regs
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class ahb_seq_item extends uvm_sequence_item;
  `uvm_object_utils(ahb_seq_item)

  // Output fields (not randomized — sampled from DUT)
       logic [31:0] HRDATA;
       logic HREADY;
       logic HRESP;

  // Input fields (driven to DUT)
  rand bit [11:0] HADDR;
  rand bit HWRITE;
  rand bit [1:0] HTRANS;
  rand bit [31:0] HWDATA;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name="ahb_seq_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    ahb_seq_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("ahb_seq_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    HRDATA = tr.HRDATA;
    HREADY = tr.HREADY;
    HRESP = tr.HRESP;
    HADDR = tr.HADDR;
    HWRITE = tr.HWRITE;
    HTRANS = tr.HTRANS;
    HWDATA = tr.HWDATA;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    ahb_seq_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("ahb_seq_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (HRDATA === tr.HRDATA);
    eq &= (HREADY === tr.HREADY);
    eq &= (HRESP === tr.HRESP);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    s = {s, $sformatf("HADDR=%0h  ", HADDR)};
    s = {s, $sformatf("HWRITE=%b  ", HWRITE)};
    s = {s, $sformatf("HTRANS=%b  ", HTRANS)};
    s = {s, $sformatf("HWDATA=%0h  ", HWDATA)};
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("HRDATA=%0h  ", HRDATA)};
    s = {s, $sformatf("HREADY=%b  ", HREADY)};
    s = {s, $sformatf("HRESP=%b  ", HRESP)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
