//----------------------------------------------------------------------
// This File: {{ register_model.frontdoor }}.svh
// Generated by: QuickUVM
// Project:  {{ project.name }}
// Author:   {{ project.author }}
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// Custom register front-door: drives the bus agent ({{ reg_bus_agent.sequence_item }})
// to perform one register access, implementing the protocol specifics that a 1:1
// uvm_reg_adapter cannot (e.g. address paging, pipelined reads, multi-byte access).
//
// Installed on every register by test_base (set_frontdoor). At runtime `rw_info`
// (uvm_reg_item) carries: kind (UVM_READ/UVM_WRITE), value[], element (the uvm_reg),
// and map. body() runs on the bus agent's sequencer.
//----------------------------------------------------------------------
{% set ba = reg_bus_agent %}

class {{ register_model.frontdoor }} extends uvm_reg_frontdoor;
  `uvm_object_utils({{ register_model.frontdoor }})

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name = "{{ register_model.frontdoor }}");
    super.new(name);
  endfunction

  virtual task body();
    {{ ba.sequence_item }} req;
    uvm_reg          rg;
    uvm_reg_addr_t   addr;
    if (!$cast(rg, rw_info.element)) begin
      `uvm_fatal("{{ register_model.frontdoor | upper }}", "front-door element is not a uvm_reg")
      return;
    end
    addr = rg.get_address(rw_info.map);   // effective address of the register in the map
    // pragma quickuvm custom frontdoor_body begin
    // Implement the protocol access here using `req` on this sequencer
    // (start_item/finish_item), `rw_info.kind`, `rw_info.value[0]`, `addr`, and
    // rg.get_n_bytes() for multi-byte registers. Set rw_info.status when done.
    // pragma quickuvm custom frontdoor_body end
  endtask

endclass
