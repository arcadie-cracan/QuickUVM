//----------------------------------------------------------------------
// This File: clusterA_env.svh
// Generated by: QuickUVM
// Project:  clusterA
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// Top (subsystem) environment — composes the child block envs and a virtual
// sequencer that collects each block's agent sequencers so a top vseq can
// coordinate stimulus across the blocks.
//----------------------------------------------------------------------

class clusterA_env extends uvm_env;
  `uvm_component_utils(clusterA_env)

  // Child block environments
  a0_env pa;
  a1_env qa;

  // Top virtual sequencer (coordinates the composed blocks)
  clusterA_virtual_sequencer vsqr;

  clusterA_env_cfg env_cfg;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    if (!uvm_config_db#(clusterA_env_cfg)::get(this, "", "env_cfg", env_cfg))
      `uvm_fatal("NOCFG", {"No clusterA_env_cfg set for: ", get_full_name()})
    pa = a0_env::type_id::create("pa", this);
    qa = a1_env::type_id::create("qa", this);
    vsqr = clusterA_virtual_sequencer::type_id::create("vsqr", this);
    // pragma quickuvm custom build_phase_additional begin
    // pragma quickuvm custom build_phase_additional end
  endfunction

  function void connect_phase(uvm_phase phase);
    super.connect_phase(phase);
    // Collect each block's active agent sequencers into the top vsqr
    vsqr.pa_ga0_sqr = pa.ga0_agnt.sqr;
    vsqr.qa_ga1_sqr = qa.ga1_agnt.sqr;
    // pragma quickuvm custom connect_phase_additional begin
    // pragma quickuvm custom connect_phase_additional end
  endfunction

endclass
