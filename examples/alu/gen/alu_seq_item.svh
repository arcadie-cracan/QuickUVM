//----------------------------------------------------------------------
// This File: alu_seq_item.svh
// Generated by: QuickUVM
// Project:  alu_tb
// Author:   qa@example.com
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class alu_seq_item extends uvm_sequence_item;
  `uvm_object_utils(alu_seq_item)

  // Output fields (not randomized — sampled from DUT)
       logic [7:0] result;
       logic zero;
       logic carry;
       logic negative;
       logic overflow;

  // Input fields (driven to DUT)
  rand bit [7:0] a;
  rand bit [7:0] b;
  rand bit [3:0] op;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name="alu_seq_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    alu_seq_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("alu_seq_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    result = tr.result;
    zero = tr.zero;
    carry = tr.carry;
    negative = tr.negative;
    overflow = tr.overflow;
    a = tr.a;
    b = tr.b;
    op = tr.op;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    alu_seq_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("alu_seq_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (result === tr.result);
    eq &= (zero === tr.zero);
    eq &= (carry === tr.carry);
    eq &= (negative === tr.negative);
    eq &= (overflow === tr.overflow);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    s = {s, $sformatf("a=%0h  ", a)};
    s = {s, $sformatf("b=%0h  ", b)};
    s = {s, $sformatf("op=%b  ", op)};
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("result=%0h  ", result)};
    s = {s, $sformatf("zero=%b  ", zero)};
    s = {s, $sformatf("carry=%b  ", carry)};
    s = {s, $sformatf("negative=%b  ", negative)};
    s = {s, $sformatf("overflow=%b  ", overflow)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
