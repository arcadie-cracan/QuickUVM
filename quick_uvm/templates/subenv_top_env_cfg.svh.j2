//----------------------------------------------------------------------
// This File: {{ dut.name }}_env_cfg.svh
// Generated by: QuickUVM
// Project:  {{ project.name }}
// Author:   {{ project.author }}
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// Top (subsystem) environment configuration — aggregates the child block
// env configs. {{ dut.name }}_base_test creates + populates it and hands each
// child env its own config via the config DB.
//----------------------------------------------------------------------

class {{ dut.name }}_env_cfg extends uvm_object;
  `uvm_object_utils({{ dut.name }}_env_cfg)

  // Child block env configuration handles (one per composed block)
{% for sv in subenvs %}
  {{ sv.env_cfg_class }} {{ sv.cfg_field }};
{% endfor %}

  // pragma quickuvm custom config_var_additional begin
  // pragma quickuvm custom config_var_additional end

  function new (string name = "{{ dut.name }}_env_cfg");
    super.new(name);
  endfunction

endclass
