//----------------------------------------------------------------------
// This File: lo_chan_vseq.svh
// Generated by: QuickUVM
// Project:  chan
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// Top (subsystem) virtual sequence — fires each composed block's default
// sequence on that block's sequencer, all concurrently. Edit the body to
// coordinate cross-block scenarios.
//----------------------------------------------------------------------

class lo_chan_vseq extends lo_chan_base_vseq;
  `uvm_object_utils(lo_chan_vseq)

  function new (string name = "lo_chan_vseq");
    super.new(name);
  endfunction

  task body;
    lo_a_seq#(8) adder_lo_a_seq;
    lo_s_seq#(8) shifter_lo_s_seq;

    adder_lo_a_seq = lo_a_seq#(8)::type_id::create("adder_lo_a_seq");
    shifter_lo_s_seq = lo_s_seq#(8)::type_id::create("shifter_lo_s_seq");
    // pragma quickuvm custom vseq_body_pre begin
    // pragma quickuvm custom vseq_body_pre end
    // Run every block's stimulus concurrently
    fork
      adder_lo_a_seq.start(p_sequencer.adder_lo_a_sqr);
      shifter_lo_s_seq.start(p_sequencer.shifter_lo_s_sqr);
    join
    // pragma quickuvm custom vseq_body_post begin
    // pragma quickuvm custom vseq_body_post end
  endtask

endclass
