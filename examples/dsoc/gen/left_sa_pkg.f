// Standalone agent VIP filelist (F2). Compiles on its own, e.g.:
//   xrun -uvm -compile -f left_sa_pkg.f
// Paths are relative to this gen/ directory.
+incdir+.
left_src_if.sv
left_sa_pkg.sv
// Extra agent sources (preserved on regen):
// pragma quickuvm custom agent_pkg_extra_files begin
// pragma quickuvm custom agent_pkg_extra_files end
