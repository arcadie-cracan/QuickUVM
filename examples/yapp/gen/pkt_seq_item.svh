//----------------------------------------------------------------------
// This File: pkt_seq_item.svh
// Generated by: QuickUVM
// Project:  yapp
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

// TB-owned enum for 'in_addr' (encodes the spec, not the DUT type).
typedef enum logic [1:0] {
  P0 = 2'd0,
  P1 = 2'd1,
  P2 = 2'd2,
  DROP = 2'd3
} in_addr_e;

class pkt_seq_item extends uvm_sequence_item;
  `uvm_object_utils(pkt_seq_item)

  // Output fields (not randomized — sampled from DUT)
       logic out0_valid;
       logic [7:0] out0_data;
       logic out1_valid;
       logic [7:0] out1_data;
       logic out2_valid;
       logic [7:0] out2_data;

  // Input fields (driven to DUT)
  rand bit in_valid;
  rand in_addr_e in_addr;
  rand bit [7:0] in_data;

  constraint trans_c {
    in_valid == 1;
  }

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name="pkt_seq_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    pkt_seq_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("pkt_seq_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    out0_valid = tr.out0_valid;
    out0_data = tr.out0_data;
    out1_valid = tr.out1_valid;
    out1_data = tr.out1_data;
    out2_valid = tr.out2_valid;
    out2_data = tr.out2_data;
    in_valid = tr.in_valid;
    in_addr = tr.in_addr;
    in_data = tr.in_data;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    pkt_seq_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("pkt_seq_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (out0_valid === tr.out0_valid);
    eq &= (out0_data === tr.out0_data);
    eq &= (out1_valid === tr.out1_valid);
    eq &= (out1_data === tr.out1_data);
    eq &= (out2_valid === tr.out2_valid);
    eq &= (out2_data === tr.out2_data);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    s = {s, $sformatf("in_valid=%b  ", in_valid)};
    s = {s, $sformatf("in_addr=%s  ", in_addr.name())};
    s = {s, $sformatf("in_data=%0h  ", in_data)};
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("out0_valid=%b  ", out0_valid)};
    s = {s, $sformatf("out0_data=%0h  ", out0_data)};
    s = {s, $sformatf("out1_valid=%b  ", out1_valid)};
    s = {s, $sformatf("out1_data=%0h  ", out1_data)};
    s = {s, $sformatf("out2_valid=%b  ", out2_valid)};
    s = {s, $sformatf("out2_data=%0h  ", out2_data)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
