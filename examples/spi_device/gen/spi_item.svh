//----------------------------------------------------------------------
// This File: spi_item.svh
// Generated by: QuickUVM
// Project:  spi_device_tb
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class spi_item extends uvm_sequence_item;
  `uvm_object_utils(spi_item)

  // Output fields (not randomized — sampled from DUT)
       logic csb;
       logic mosi;
       logic [7:0] rx_byte;
       logic [7:0] frame_cnt;

  // Input fields (driven to DUT)
  rand bit miso;

  // pragma quickuvm custom class_item_additional begin
  // The byte this device will shift out on the next frame. Not a DUT port — it is the
  // device's own payload, so it lives here rather than in `ports:`.
  rand bit [7:0] miso_byte;
  // pragma quickuvm custom class_item_additional end

  function new (string name="spi_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    spi_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("spi_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    csb = tr.csb;
    mosi = tr.mosi;
    rx_byte = tr.rx_byte;
    frame_cnt = tr.frame_cnt;
    miso = tr.miso;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    spi_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("spi_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (csb === tr.csb);
    eq &= (mosi === tr.mosi);
    eq &= (rx_byte === tr.rx_byte);
    eq &= (frame_cnt === tr.frame_cnt);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    s = {s, $sformatf("miso=%b  ", miso)};
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("csb=%b  ", csb)};
    s = {s, $sformatf("mosi=%b  ", mosi)};
    s = {s, $sformatf("rx_byte=%0h  ", rx_byte)};
    s = {s, $sformatf("frame_cnt=%0h  ", frame_cnt)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
