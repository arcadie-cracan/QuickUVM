//----------------------------------------------------------------------
// This File: vu_seq_item.svh
// Generated by: QuickUVM
// Project:  vec_unit
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

// TB-owned packed struct for 'hdr' (nested structs → named typedefs).
typedef struct packed {
  bit [3:0] cls;
  bit [3:0] id;
} hdr_tag_t;

typedef struct packed {
  hdr_tag_t tag;
  bit en;
} hdr_t;

class vu_seq_item extends uvm_sequence_item;
  `uvm_object_utils(vu_seq_item)

  // Output fields (not randomized — sampled from DUT)
       logic [9:0] sum;
       logic [7:0] tag_out;

  // Input fields (driven to DUT)
  rand hdr_t hdr;
  rand bit [3:0][7:0] lanes;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name="vu_seq_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    vu_seq_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("vu_seq_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    sum = tr.sum;
    tag_out = tr.tag_out;
    hdr = tr.hdr;
    lanes = tr.lanes;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    vu_seq_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("vu_seq_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (sum === tr.sum);
    eq &= (tag_out === tr.tag_out);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    s = {s, $sformatf("hdr=%0h  ", hdr)};
    s = {s, $sformatf("lanes=%0h  ", lanes)};
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("sum=%0h  ", sum)};
    s = {s, $sformatf("tag_out=%0h  ", tag_out)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
