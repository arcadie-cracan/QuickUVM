//----------------------------------------------------------------------
// This File: d_agent.svh
// Generated by: QuickUVM
// Project:  dp
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class d_agent #(parameter int W = 8) extends uvm_agent;
  `uvm_component_param_utils(d_agent#(W))

  // is_active is inherited from uvm_agent; set from cfg in build_phase
  d_cfg#(W)  cfg;
  virtual d_if#(W) vif;

  uvm_analysis_port #(d_seq_item#(W)) ap;
  d_driver#(W)     drv;
  d_monitor#(W)    mon;
  d_sequencer#(W)  sqr;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  virtual function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    if (!uvm_config_db#(d_cfg#(W))::get(this, "", "cfg", cfg))
      `uvm_fatal("NOCFG", {"No d_cfg set for: ", get_full_name()})
    is_active = cfg.is_active;
    vif       = cfg.vif;
    if (is_active == UVM_ACTIVE) begin
      drv = d_driver#(W)::type_id::create("drv", this);
      sqr = d_sequencer#(W)::type_id::create("sqr", this);
    end
    mon = d_monitor#(W)::type_id::create("mon", this);
    ap  = new("ap", this);
    // pragma quickuvm custom build_phase_additional begin
    // pragma quickuvm custom build_phase_additional end
  endfunction

  virtual function void connect_phase(uvm_phase phase);
    super.connect_phase(phase);
    if (is_active == UVM_ACTIVE) begin
      drv.seq_item_port.connect(sqr.seq_item_export);
      drv.vif = vif;
    end
    mon.ap.connect(ap);
    mon.vif = vif;
    // pragma quickuvm custom connect_phase_additional begin
    // pragma quickuvm custom connect_phase_additional end
  endfunction

endclass
