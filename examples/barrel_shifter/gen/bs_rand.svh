//----------------------------------------------------------------------
// This File: bs_rand.svh
// Generated by: QuickUVM
// Project:  barrel_shifter_tb
// Author:   qa@example.com
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// S2 library sequence — kind 'random' for agent 'bs'.

class bs_rand extends uvm_sequence #(bs_seq_item);
  `uvm_object_utils(bs_rand)

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name = "bs_rand");
    super.new(name);
  endfunction

  // Item count — a test may override it via `sequence: {... count: N}` (S2).
  int unsigned count = 256;

  task body;
    bs_seq_item tr = bs_seq_item::type_id::create("tr");
    `uvm_info("body", "bs_rand (random): starting", UVM_FULL)
    repeat(count) do_item(tr);
  endtask

  task do_item (bs_seq_item tr);
    start_item(tr);
    if (!(tr.randomize() with {

      // pragma quickuvm custom do_item_constraints begin
      // pragma quickuvm custom do_item_constraints end
    })) `uvm_fatal("BS_RAND", "bs_rand randomization failed")
    `uvm_info("do_item", tr.input2string(), UVM_FULL)
    finish_item(tr);
  endtask

endclass
