//----------------------------------------------------------------------
// This File: rd_drain.svh
// Generated by: QuickUVM
// Project:  cdc_fifo
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------
// S2 library sequence — kind 'random' for agent 'rd'.

class rd_drain extends uvm_sequence #(rd_item);
  `uvm_object_utils(rd_drain)

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name = "rd_drain");
    super.new(name);
  endfunction

  // Item count — a test may override it via `sequence: {... count: N}` (S2).
  int unsigned count = 40;

  task body;
    rd_item tr = rd_item::type_id::create("tr");
    `uvm_info("body", "rd_drain (random): starting", UVM_FULL)
    repeat(count) do_item(tr);
  endtask

  task do_item (rd_item tr);
    start_item(tr);
    if (!(tr.randomize() with {

      // pragma quickuvm custom do_item_constraints begin
      // pragma quickuvm custom do_item_constraints end
    })) `uvm_fatal("RD_DRAIN", "rd_drain randomization failed")
    `uvm_info("do_item", tr.input2string(), UVM_FULL)
    finish_item(tr);
  endtask

endclass
