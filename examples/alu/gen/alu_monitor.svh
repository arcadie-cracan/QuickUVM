//----------------------------------------------------------------------
// This File: alu_monitor.svh
// Generated by: QuickUVM
// Project:  alu_tb
// Author:   qa@example.com
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class alu_monitor extends uvm_monitor;
  `uvm_component_utils(alu_monitor)

  virtual alu_if vif;

  uvm_analysis_port #(alu_seq_item) ap;

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    super.build_phase(phase);
    ap = new("ap", this);
  endfunction

  task run_phase(uvm_phase phase);
    alu_seq_item tr;
    forever begin
      sample_dut(tr);
      ap.write(tr);
    end
  endtask

  task sample_dut (output alu_seq_item tr);
    alu_seq_item t;
    t = alu_seq_item::type_id::create("t");
    //---------------------------------------------
    // Combinational DUT (0-cycle latency): one race-free snapshot of inputs AND
    // outputs taken together through the monitor clocking block.
    //---------------------------------------------
    @vif.mon_cb;
    t.result = vif.mon_cb.result;
    t.zero = vif.mon_cb.zero;
    t.carry = vif.mon_cb.carry;
    t.negative = vif.mon_cb.negative;
    t.overflow = vif.mon_cb.overflow;
    t.a = vif.mon_cb.a;
    t.b = vif.mon_cb.b;
    void'($cast(t.op, vif.mon_cb.op));

    // pragma quickuvm custom sample_dut_additional begin
    // pragma quickuvm custom sample_dut_additional end

    tr = t;
    `uvm_info("sample_dut", tr.convert2string(), UVM_FULL)
  endtask

endclass
