//----------------------------------------------------------------------
// This File: {{ agent.default_seq_name }}.svh
// Generated by: QuickUVM
// Project:  {{ project.name }}
// Author:   {{ project.author }}
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class {{ agent.default_seq_name }}{{ agent.param_decl }} extends uvm_sequence #({{ agent.sequence_item }}{{ agent.param_args }});
  `uvm_object_{{ 'param_' if agent.parameters else '' }}utils({{ agent.default_seq_name }}{{ agent.param_args }})

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name = "{{ agent.default_seq_name }}");
    super.new(name);
  endfunction

  task body;
    {{ agent.sequence_item }}{{ agent.param_args }} tr = {{ agent.sequence_item }}{{ agent.param_args }}::type_id::create("tr");
    `uvm_info("body", "{{ agent.default_seq_name }}: about to start do_item loop", UVM_FULL)
    repeat({{ test.num_items }}) do_item(tr);
  endtask

  task do_item ({{ agent.sequence_item }}{{ agent.param_args }} tr);
    `uvm_info("do_item", "executing", UVM_FULL)
    start_item(tr);
    if (!(tr.randomize() with {
{% if agent_driven_reset %}
      tr.{{ agent_driven_reset.name }}=='{% if agent_driven_reset.active_low %}1{% else %}0{% endif %};
{% endif %}

      // pragma quickuvm custom do_item_constraints begin
      // pragma quickuvm custom do_item_constraints end
    })) `uvm_fatal("{{ agent.default_seq_name | upper }}", "{{ agent.default_seq_name }} randomization failed")
    `uvm_info("do_item", tr.input2string(), UVM_FULL)
    finish_item(tr);
  endtask

endclass
