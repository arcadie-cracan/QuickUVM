//----------------------------------------------------------------------
// This File: es_item.svh
// Generated by: QuickUVM
// Project:  es_adaptp_tb
// Author:   qa@example.com
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class es_item extends uvm_sequence_item;
  `uvm_object_utils(es_item)

  // Output fields (not randomized — sampled from DUT)
       logic window_done;
       logic [15:0] ones_cnt;
       logic test_fail;
       logic alert;

  // Input fields (driven to DUT)
  rand bit [3:0] sample;
  rand bit valid;

  constraint trans_c {
    valid == 1;
  }

  // pragma quickuvm custom class_item_additional begin
  // pragma quickuvm custom class_item_additional end

  function new (string name="es_item");
    super.new(name);
  endfunction

  function void do_copy(uvm_object rhs);
    es_item tr;
    if(!$cast(tr, rhs)) `uvm_fatal("es_item", "ILLEGAL do_copy() cast")
    super.do_copy(rhs);
    window_done = tr.window_done;
    ones_cnt = tr.ones_cnt;
    test_fail = tr.test_fail;
    alert = tr.alert;
    sample = tr.sample;
    valid = tr.valid;

    // pragma quickuvm custom do_copy_additional begin
    // pragma quickuvm custom do_copy_additional end
  endfunction

  function bit do_compare(uvm_object rhs, uvm_comparer comparer);
    es_item tr;
    bit eq;
    if(!$cast(tr, rhs)) `uvm_fatal("es_item", "ILLEGAL do_compare() cast")
    eq  = super.do_compare(rhs, comparer);
    eq &= (window_done === tr.window_done);
    eq &= (ones_cnt === tr.ones_cnt);
    eq &= (test_fail === tr.test_fail);
    eq &= (alert === tr.alert);

    // pragma quickuvm custom do_compare_additional begin
    // pragma quickuvm custom do_compare_additional end
    return(eq);
  endfunction


  function void do_print(uvm_printer printer);
    $display("\n\n\t\t*** print()/sprint() not implemented for this transaction type ***\n\n");
  endfunction

  virtual function string input2string();
    string s = "";
    s = {s, $sformatf("sample=%b  ", sample)};
    s = {s, $sformatf("valid=%b  ", valid)};
    return(s);
  endfunction

  virtual function string output2string();
    string s = "";
    s = {s, $sformatf("window_done=%b  ", window_done)};
    s = {s, $sformatf("ones_cnt=%0h  ", ones_cnt)};
    s = {s, $sformatf("test_fail=%b  ", test_fail)};
    s = {s, $sformatf("alert=%b  ", alert)};
    return(s);
  endfunction

  virtual function string convert2string();
    // pragma quickuvm custom convert2string_additional begin
    return({input2string(), "  ", output2string()});
    // pragma quickuvm custom convert2string_additional end
  endfunction

endclass
