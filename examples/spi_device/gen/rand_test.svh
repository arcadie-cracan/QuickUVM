//----------------------------------------------------------------------
// This File: rand_test.svh
// Generated by: QuickUVM
// Project:  spi_device_tb
// Author:   QuickUVM
// SPDX-License-Identifier: MIT
//----------------------------------------------------------------------

class rand_test extends spi_host_base_test;
  `uvm_component_utils(rand_test)

  function new (string name, uvm_component parent);
    super.new(name, parent);
  endfunction

  function void build_phase(uvm_phase phase);
    // pragma quickuvm custom build_phase_additional begin
    // pragma quickuvm custom build_phase_additional end
    super.build_phase(phase);
  endfunction

  task run_phase(uvm_phase phase);
    // Reactive bench: the DUT is the INITIATOR and the responder agent(s) serve it, so
    // there is no TB-initiated stimulus. The test must NOT start a sequence on a
    // responder's sequencer — its forever responder sequence owns it, and a second
    // sequence there would clobber the computed responses with random items.
    //----------------------------------------------------
    phase.raise_objection(this);
    `uvm_info("rand_test", "reactive bench: letting the DUT run", UVM_FULL)
    // The DUT drives this agent's clock (`source: dut`), so we must NOT bound the test on
    // its edges. Two ways that goes wrong: the clock is typically GATED (an SPI sck only
    // ticks inside a frame), so N edges is not N transfers; and if the DUT never drives it
    // at all the test HANGS — and a hung bench can never report an error. A wall-clock
    // bound always terminates, so a dead clock comes out RED instead of never coming out.
    #(200 * 40);
    // pragma quickuvm custom run_phase_additional begin
    // pragma quickuvm custom run_phase_additional end
    phase.drop_objection(this);
  endtask

endclass
